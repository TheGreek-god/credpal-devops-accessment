# ================================
# PostgreSQL Database Configuration
# ================================
DB_HOST=postgres         # Matches the service name in docker-compose
DB_PORT=5432
DB_USER=credpal          # Example username
DB_PASSWORD=changeme     # Placeholder password (replace in .env)
DB_NAME=credpaldb